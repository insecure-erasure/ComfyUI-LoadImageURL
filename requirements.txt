# Core dependencies (usually already installed with ComfyUI)
torch>=2.0.0
pillow>=9.0.0
numpy>=1.20.0
requests>=2.28.0

# Optional but recommended for enhanced security
imageio>=2.31.0

# Note: These are typically already installed with ComfyUI
# If installing manually: pip install -r requirements.txt
